{{- if .Values.eventstore.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hume-helm.fullname" . }}-eventstore-configmap
  labels:
  {{- include "hume.labels" . | nindent 4 }}
  {{- with .Values.annotations }}
  annotations:
  {{ toYaml . | indent 4 }}
  {{- end }}
data:
  SPRING_DATASOURCE_URL: "jdbc:postgresql://postgresql-eventstore:{{ .Values.postgresqlEventStore.global.postgresql.auth.servicePort }}/{{ .Values.postgresqlEventStore.global.postgresql.auth.database }}"
  SPRING_DATASOURCE_USERNAME: {{ .Values.postgresqlEventStore.global.postgresql.auth.username }}
  SPRING_DATASOURCE_PASSWORD: {{ .Values.postgresqlEventStore.global.postgresql.auth.password }}
{{- end }}
---