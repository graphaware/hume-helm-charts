apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hume-orchestra.fullname" . }}-test-connection"
  labels:
    {{- include "hume-orchestra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hume-orchestra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
